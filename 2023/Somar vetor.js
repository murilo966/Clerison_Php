function Somar_entre(vet) {
    var aux = 0
    for (var i = 0; i < vet.length; i++) {
        aux = aux+vet[i]
        console.log(aux)
    }
    if (i == vet.length) {
        console.log("valor total é: "+aux)
    }
}
